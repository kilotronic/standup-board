# Single-replica presence board. Do NOT scale (one SQLite writer per process).
# Device-flow `standup login` needs no public callback, so OAUTH_REDIRECT_URL is
# optional for a pure-LAN deploy. The named volume below keeps the board across
# `docker compose down`/`up`; drop it for a purely ephemeral deploy.
services:
  standup:
    build: .
    ports:
      - "${PORT:-8080}:8080"
    environment:
      SECRET_KEY: ${SECRET_KEY:?set SECRET_KEY}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID:?set GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET:?set GITHUB_CLIENT_SECRET}
      GITHUB_ALLOWED_USERS: ${GITHUB_ALLOWED_USERS:-}
      OAUTH_REDIRECT_URL: ${OAUTH_REDIRECT_URL:-}
      COOKIE_SECURE: ${COOKIE_SECURE:-0}
    volumes:
      - standup-data:/var/lib/standup
    restart: unless-stopped

volumes:
  standup-data:
